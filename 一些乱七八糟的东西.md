# Clutter

## 类  

1. 如果类中需要使用const static变量，其初始化很特殊，可以直接在声明时初始化：

   ```c++
   class Ctest
   {
   private:
       const static int PI = 3; //声明时直接赋值
       const static double PI = 3.14;  //ERROR！因为历史遗留问题，浮点数型下应该使用constexpr
       constexpr static float PI = 3.1415926; // 正确的！
   };
   ```

2. 